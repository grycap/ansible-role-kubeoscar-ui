apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: oscar-ui
spec:
  template:
    metadata:
      labels:
        app: oscar-ui
    spec:
      containers:
        - name: oscar-ui
          image: grycap/oscar-ui
          

  
  
